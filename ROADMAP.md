# עולם האותיות — תוכנית פיתוח

> כל שלב הוא יחידה עצמאית שאפשר לבדוק ולהתחיל לשחק בה מיד לאחר הסיום.

---

## שלב 0 — יסודות ✅ (הושלם)

- [x] ניקוי קבצים: קובץ HTML יחיד
- [x] תיקון אותיות חסרות לעולמות 4–5 (כ, ל, מ, נ, ס)
- [x] תיקון ספירת מטבעות כפולה
- [x] תיקון סלקטורי CSS שבורים באווטר
- [x] מיזוג לגרסה אחת עם UI מלוטש

## שלב 0.5 — תיקוני משחק נפילה ✅ (הושלם, מאי 2026)

- [x] שחקן מתחיל בתחתית המסך (במקום בראש)
- [x] תנועה מלאה: שמאל/ימין/מעלה/מטה — touch, mouse, keyboard
- [x] איפוס שחקן לתחתית + ניקוי מכשולים אחרי כל פגיעה
- [x] אווטר מלא (תמונה, חולצה, מכנסיים) מוצג בתוך המשחק
- [x] הסרת אפשרויות שיער וחצאית מבניית האווטר
- [x] TTS: Google Translate TTS לעברית אמינה במובייל/טאבלט
- [x] שאלת הצלה: הקראת שם האות בלבד (ללא טקסט גלוי)
- [x] תיקון מיקום דמות: נראית במלואה ב-75% מגובה המסך בכל מכשיר (כולל iPhone עם safe-area)

---

## שלב 0.6 — תיקוני באגים (לפני POC)

**מטרה:** לסגור באגים שפוגעים ישירות בחוויית המשחק לפני שיוצא לפידבקים  
**משך משוער:** 3–4 שעות

### באגים:

**🐛 באג 1 — המשחק ממשיך לרוץ בזמן שאלת ההצלה**
- כרגע: כשמופיעה שאלת הצלה באמצע משחק הנפילה, הלולאה (`requestAnimationFrame`) ממשיכה לרוץ ברקע
- תיקון: `pause` מלא של לולאת המשחק ברגע שמופיעה השאלה — שחקן קפוא במקומו, מכשולים קפואים
- חזרה: כשהשחקן מקבל חיים וחוזר למשחק — **ממשיכים בדיוק מאותה נקודה** (אותו מיקום X/Y, אותם מכשולים) ולא מאפסים מחדש

**🐛 באג 2 — כפתורי תשובה על גבי מסך המשחק**
- כרגע: כפתורי הבחירה (4 אפשרויות) מופיעים באזור שחופף חלקית לאלמנטי המשחק
- תיקון: כפתורי התשובה צריכים להיות **overlay מלא על גבי המסך** — בשני המשחקים (נפילה + מכוניות המירוץ) — ולא להסתיר את השחקן, אבל ברור שהמשחק "בהמתנה"

**🐛 באג 3 — כפתור "הבא" לא ברור במסך האותיות**
- כרגע: הכפתור לא בולט מספיק — ילד לא מבין שצריך ללחוץ עליו להמשיך
- תיקון: עיצוב מחדש של הכפתור — גדול יותר, צבע בולט, אולי טקסט + חץ, אנימציית pulse קלה

---

## שלב 1 — השלמת האלפבית ✅ (הושלם, מאי 2026)

**מטרה:** כל 22 האותיות זמינות במשחק  

### מה בוצע:
- [x] כל 22 האותיות ב-`LETTERS` (א–ת) עם שם + מילה לדוגמה
- [x] עולמות 6–10 מוגדרים ב-`WORLDS` עם כל 22 האותיות
- [x] כל 5 הטירות במפה מציגות את כל 22 האותיות
- [x] `TRICK_QS` מכיל שאלות לכל 22 האותיות
- [x] תיקון קריסה ב-`endLetters()` (עדכון כוכבים לפי טירה)

---

## שלב 2 — שמירת התקדמות (localStorage) ✅ (הושלם, מאי 2026)

**מטרה:** ילד שסוגר וחוזר רואה את הכוכבים שלו

### מה בוצע:
- [x] מסך בחירת שחקן (`s-players`) — עד 3 פרופילים
- [x] שמירה ב-`localStorage` (מפתח `letter_game_v1`): שם, צבעים, תמונה, מטבעות, חיים, כוכבים
- [x] שחקן חדש → אווטר בילדר → נשמר אוטומטית
- [x] טעינת שחקן קיים → "המשך" → נפתחת המפה עם כל הנתונים
- [x] שמירה אחרי סיום עולם (`endLetters`)
- [x] שמירה אחרי שאלת הצלה נכונה (`checkTrick`)
- [x] שמירה כשמרוויחים חיים מהטיימר
- [x] מחיקת שחקן עם אישור inline

---

## שלב 2.5 — חיבור מסך הניצחון לזרימת המשחק

**מטרה:** מסך `s-win` קיים כ-HTML ועוצב, אך `goScreen('s-win')` לא נקרא אף פעם ב-JS — המסך "מנותק"
**משך משוער:** 1–2 שעות

### צעדים:
1. מצא את `endLetters()` — הפונקציה שמסיימת עולם
2. החלף `goScreen('s-map')` ב-`goScreen('s-win')` בסיום מוצלח
3. מלא את שדות המסך: `win-subtitle` (שם האות/עולם), `win-earned` (יהלומים), `win-correct`, `win-stars-count`, `win-streak`
4. כפתור "שחק שוב" יפעיל מחדש את אותו עולם
5. כפתור "חזרה למפה" יעבור ל-`s-map`

---

## שלב 3 — מסך לימוד לפני כל עולם

**מטרה:** לפני המשחק, הילד רואה ומאזין לאותיות של העולם  
**משך משוער:** 3–4 שעות

### צעדים:
1. צור מסך חדש `#s-learn` בין הבחירת עולם לתחילת המשחק
2. הצג כל אות של העולם בגדול עם:
   - שם האות
   - מילה לדוגמה
   - כפתור שמע שמפעיל TTS
3. הוסף כפתור "הבנתי, בואו נשחק!" שעובר למשחק
4. עדכן את `launchGame()` לעבור דרך מסך הלימוד לפני המשחק

---

## שלב 3.5 — שדרוג מסך ההגדרות

**מטרה:** הגדרות שלמות ושימושיות  
**משך משוער:** 3–4 שעות

### פיצ'ר א — רמזים

טוגל "רמזים" בהגדרות שמשפיע על המשחק בפועל:
- כשרמזים פעילים: לפני שמופיעות 4 אפשרויות, האות מוקראת + מוצגת לשנייה אחת, ואז נעלמת
- כשרמזים כבויים: שום עזרה — האות מוקראת בלי הצגה חזותית
- שמור ב-localStorage

### פיצ'ר ב — ערכות עולם (סקינים)

בחירת ערכת עיצוב בהגדרות שמשנה את מראה המשחק:
- **Classic** (ברירת מחדל) — צבעים כחול/כהה, כיכר הנוכחית
- **Unicorn Magic** — גוונים ורוד/סגול, כוכבים, חד-קרנים
- **Space Explorer** — שחור חלל, כוכבים, כחול נאון
- **Ocean Adventure** — טורקיז, גלים, דגים

טכנית: כל ערכה מגדירה ערכי CSS variables (רקע, צבעי בלוקים, צבע כפתורים).  
שמור בחירה ב-localStorage.

---

## שלב 4 — שיפורי UX ואנימציות

**מטרה:** תחושה חלקה ומרגשת יותר  
**משך משוער:** 3–4 שעות

### צעדים:
1. **מעברים בין מסכים** — הוסף fadeIn/slideIn ב-CSS
2. **אנימציית ניצחון** — קונפטי נופל כשמסיימים עולם
3. **אפקט נכון/שגוי** — ניעור + אנימציה על כפתורי הבחירה (כבר חלקית קיים)
4. **כוכבים מונפשים** — הכוכבים על המפה מופיעים בהדרגה לאחר ניצחון
5. **משוב קולי מגוון** — לפחות 5 משפטי עידוד שונים (לא רק "כל הכבוד")

---

## שלב 5 — מיני-משחק שלישי: סדר האותיות

**מטרה:** הילד מסדר אותיות בסדר הנכון  
**משך משוער:** 4–5 שעות

### צעדים:
1. הוסף כפתור "משחק הסדר" ב-popup של כל עולם
2. הצג 3–5 אותיות בלוקיות בסדר מבולבל
3. הילד גורר אותן (drag & drop) לסדר הנכון
4. בדיקה ופידבק — מוזיקה ויהלומים בסיום נכון
5. כתוב `initOrderGame()` ו-`checkOrder()` בהמשך הקוד הקיים

---

## שלב 6 — מצב הורה + מעקב הישגים

**מטרה:** הורה רואה ניתוח מעמיק של הלמידה, מוסיף מילים, ומכוון את הילד  
**משך משוער:** 5–6 שעות

### צעדים:
1. הוסף כפתור "מצב הורה" במסך הפתיחה (קטן, בפינה)
2. בקש קוד פשוט (PIN 4 ספרות) לגישה
3. **מסך סטטיסטיקות מפורטות:**
   - לכל אות: כמה פעמים ענו נכון / שגוי (אחוז הצלחה)
   - גרף התקדמות לאורך זמן — אילו אותיות השתפרו
   - הדגשה ויזואלית של אותיות חלשות (צריכות חזרה)
   - עולמות שהושלמו + זמן משחק כולל
4. **ניהול מילים מותאמות אישית:**
   - הורה מוסיף מילים לבנק המילים (למשחק תמונה→כתיב — שלב 8)
   - לכל מילה: טקסט + URL לתמונה (או בחירה מגלריה מובנית)
   - ניהול: הוסף / ערוך / מחק
5. שמור הכל ב-localStorage

---

## שלב 7 — נגישות ופוליש

**מטרה:** המשחק עובד טוב יותר על יותר מכשירים  
**משך משוער:** 2–3 שעות

### צעדים:
1. **גדלי פונט responsive** — שימוש ב-`clamp()` בכל הטקסטים הקטנים
2. **orientation lock** — הצג הודעה להטיית המכשיר ל-landscape אם המשחק קורס
3. **בדיקת iOS** — ודא ש-Web Speech API ו-AudioContext עובדים ב-Safari
4. **כפתורים גדולים יותר** — minimal tap target של 48px בכל הממשק
5. **PWA בסיסי** — הוסף `manifest.json` ו-service worker פשוט כדי שאפשר "להתקין" על הבית

---

## שלב 8 — משחק כתיב: תמונה + 4 אפשרויות

**מטרה:** הילד רואה תמונה של מילה, שומע אותה, ובוחר את הכתיב הנכון מ-4 אפשרויות  
**משך משוער:** 4–5 שעות

### רעיון המשחק:
- מציגים תמונה גדולה (למשל: 🦁 אריה)
- TTS מקריא את המילה
- 4 כפתורים עם אפשרויות כתיב (אחת נכונה + 3 מבלבלות)
- פידבק מיידי + ניקוד יהלומים

### צעדים:
1. צור מסך `#s-spelling` עם תמונה גדולה + 4 כפתורי כתיב
2. הגדר `SPELLING_WORDS` — מערך ברירת מחדל של ~30 מילים עם תמונות (emoji או URL)
3. **TTS:** הקרא את המילה אוטומטית עם הופעת המסך + כפתור "שמע שוב"
4. בנה לוגיקת בחירת תשובות מבלבלות — אותיות דומות קרוב לנכון (למשל: "אריה" / "אריא" / "ארייה" / "אריהה")
5. **אינטגרציה עם פאנל הורים (שלב 6):** אם הורה הוסיף מילים מותאמות — אלה מופיעות גם הן
6. שמור סטטיסטיקות per-word ב-localStorage (לפאנל הורים)

### שאלה פתוחה:
- האם להקריא את המילה אוטומטית? **המלצה: כן** — זה מחזק את הקישור בין שמיעה לכתיב

---

## סדר עדיפויות מעודכן (מאי 2026)

| עדיפות | שלב | ערך |
|---|---|---|
| ✅ הושלם | שלב 0–2 + 0.6 | יסודות, אלפבית, שמירה, תיקוני באגים |
| 🔴 1 | **עיצוב חדש למשחק הנפילה** | שדרוג ויזואלי — תחושת משחק אמיתי |
| 🔴 2 | **שלב 8 — משחק כתיב (תמונה→מילה)** | מיני-משחק חדש עם ערך פדגוגי גבוה |
| 🔴 3 | **שלב 5 — מיני-משחק: סדר האותיות** | drag & drop — מעורבות + לימוד רצף |
| 🟡 4 | שלב 4 — UX ואנימציות | מעברים, קונפטי, כוכבים מונפשים |
| 🟡 5 | שלב 2.5 — מסך ניצחון | חיבור `s-win` לזרימת המשחק |
| 🟡 6 | שלב 3 — מסך לימוד | הילד רואה ומאזין לאותיות לפני משחק |
| 🟢 7 | שלב 6 — פאנל הורים + מעקב | כלי להורה + בסיס לשלב 8 |
| 🟢 8 | שלב 7 — נגישות ופוליש | responsive, PWA, iOS |
